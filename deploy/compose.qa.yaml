services:
  caddy:
    volumes:
      - type: bind
        source: ./deploy/caddy/Caddyfile.qa
        target: /etc/caddy/Caddyfile
        read_only: true
    ports: !override
      - target: 443
        published: 443
        host_ip: 127.0.0.1
        protocol: tcp
